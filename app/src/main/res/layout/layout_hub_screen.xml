<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/hubScrollView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/sticky_background_light"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:id="@+id/tv_greeting"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/hub_greeting"
            android:textColor="@color/sticky_text"
            android:textSize="24sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            android:alpha="0.7"
            android:text="@string/hub_subtitle"
            android:textColor="@color/sticky_text"
            android:textSize="14sp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_hub_create_new"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:contentDescription="@string/desc_btn_create_new"
            android:elevation="4dp"
            android:minHeight="48dp"
            android:text="@string/hub_btn_create"
            android:textAllCaps="false"
            android:textColor="@color/sticky_text"
            android:textSize="16sp"
            android:textStyle="bold"
            app:backgroundTint="@color/sticky_yellow"
            app:cornerRadius="16dp"
            app:icon="@drawable/ic_plus"
            app:iconGravity="textStart"
            app:iconPadding="8dp"
            app:iconSize="20dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:layout_marginBottom="12dp"
            android:text="@string/hub_title_active_notes"
            android:textColor="@color/sticky_text"
            android:textSize="18sp"
            android:textStyle="bold" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_hub_gallery"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clipToPadding="false"
            android:contentDescription="@string/desc_rv_notes"
            android:minHeight="120dp"
            android:paddingBottom="20dp" />

        <TextView
            android:id="@+id/tv_empty_state"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:alpha="0.5"
            android:gravity="center"
            android:text="@string/hub_empty_state"
            android:visibility="gone" />

    </LinearLayout>
</ScrollView>